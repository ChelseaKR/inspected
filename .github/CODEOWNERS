# Routing for the paths where a bad change is hardest to see.
#
# `require_code_owner_review` is not enabled: a self-review is not code-owner approval and
# this repository has one maintainer. The routing file is kept anyway. It is what a second
# maintainer turns on, and it records which paths are the ones to read twice.

*                       @ChelseaKR

/.github/workflows/     @ChelseaKR   # workflow edits are the poisoned-pipeline surface
/.github/dependabot.yml @ChelseaKR
/SECURITY.md            @ChelseaKR

# The three files that decide what this project says about somebody.
/src/inspected/artifacts.py  @ChelseaKR   # the publication rules
/src/inspected/intervals.py  @ChelseaKR   # the only route a proportion takes to output
/src/inspected/sources.py    @ChelseaKR   # reviewed endpoints and quoted caveats
