# SSH allowed-signers list, read by `git verify-tag` in .github/workflows/release.yml.
#
# Format, one principal per line:
#   <email> namespaces="git" <keytype> <base64 key>
#
# The principal below is the maintainer's release-signing key, checked against the
# GitHub API (users/ChelseaKR/ssh_signing_keys) on 2026-08-17, where it is recorded
# as "outcome-receipts release signing". Public keys only; nothing secret belongs in
# this file. A tag signed by any other key does not verify, and the workflow still
# refuses to run if this file ever lists no principal.
3114598+ChelseaKR@users.noreply.github.com namespaces="git" ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIIOjn/aI0d9jElCoi7qT4+6j9UmgTH7vLGAqJXiKMoCE
